// Generated by CoffeeScript 1.6.1
(function() {

  app.moveto = {
    init: function($element) {
      var that, width;
      this.el = $element;
      this.movers = $element;
      this.breakpoint = 767;
      this.eventListeners();
      this.staticWidth = window.staticWidth;
      that = this;
      width = this.staticWidth ? this.staticWidth : window.innerWidth;
      return that.testSize(width);
    },
    eventListeners: function() {
      var that;
      that = this;
      return $(window).resize(function() {
        return that.testSize(window.innerWidth);
      });
    },
    testSize: function(width) {
      if (width >= this.breakpoint && (this.currentViewport === "narrow" || (this.currentViewport == null))) {
        return this.viewpoitAction("wide");
      } else if (width < this.breakpoint && (this.currentViewport === "wide" || (this.currentViewport == null))) {
        return this.viewpoitAction("narrow");
      }
    },
    viewpoitAction: function(size) {
      this.currentViewport = size;
      return this.movers.each(function() {
        var $this;
        $this = $(this);
        return $this.appendTo($this.attr("data-target" + size));
      });
    }
  };

}).call(this);
