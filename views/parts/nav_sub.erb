<div class="nav__sub">
  <ul>
    <% @boards.each do |board| %>
      <li><a class="nav__item <%= "is-active" if @active_subpage == board.slug %>" href="/products/<%= board.slug %>"><%= board.name %></a>
    <% end %>
  </ul>
</div>