
<div class="product-info Constrained Constrained--tight">

  <article>
    <h1><%= @board.name %></h1>
    <p>Urskog’s new model Stubbe is a dropthrough board perfect for freeriding. We made it slightly shorter than the average dropthrough model which gives a great turn ratio without comprimising on its stability.</p>

    <table class="is-fixed">
      <tr>
        <td class="u-size1of3"><strong>Wood:</strong>
        <td class="u-size2of3"><%= @board.wood_list %>
      </tr>
      <tr>
        <td class="u-size1of3"><strong>Lenght:</strong>
        <td class="u-size1of3">950mm
        <td class="u-size1of3">37,4”
      </tr>
      <tr>
        <td class="u-size1of3"><strong>Width:</strong>
        <td class="u-size1of3">225mm
        <td class="u-size1of3">8,9”
      </tr>
      <tr>
        <td class="u-size1of3"><strong>Wheelbase:</strong>
        <td class="u-size1of3">690mm <td class="u-size1of3">27,1”
      </tr>
    </table>
  </article>

  <aside class="Shop js-displayOption u-marginTm u-pullEnd">

  <form action="/products/<%= @board.slug %>/buy">
    <% if @board.deck_only? %>

    <label class="Control" for="deck-only">
      <input type="radio" id="deck-only" name="buy_option" value="deck" checked> <span>Deck only</span> <span class="Control-sub"><%= @board.price.deck %> <small>SEK</small></span>
    </label>

    <div class="Shop-option js-option js-deck-only">
      <button class="Button Shop-buyButton"><span>Buy<strong class="Button-sub"><%= @board.price.deck %> <small>SEK</small></strong></span></button>
      <p>Free shipping worldwide</p>
    </div>

    <% else %>

    <label class="Control u-marginRl u-paddingRm" for="complete">
      <input type="radio" id="complete" name="buy_option" value="complete" checked> <span>Complete</span> <span class="Control-sub"><%= @board.price.complete %> <small>SEK</small></span>
    </label>

    <label class="Control" for="deck-only">
      <input type="radio" id="deck-only" name="buy_option" value="deck"> <span>Deck only</span> <span class="Control-sub"><%= @board.price.deck %> <small>SEK</small></span>
    </label>

    <div class="Shop-option js-option js-complete">
      <button class="Button Shop-buyButton"><span>Buy<strong class="Button-sub"><%= @board.price.complete %> <small>SEK</small></strong></span></button>
      <p>Free shipping worldwide</p>
    </div>
    <div class="Shop-option js-option js-deck-only u-isHidden">
      <button class="Button Shop-buyButton"><span>Buy<strong class="Button-sub"><%= @board.price.deck %> <small>SEK</small></strong></span></button>
      <p>Free shipping worldwide</p>
    </div>

    <% end %>
  
  </form>

  </aside>

</div>


<ul class="ProductList">
  <% @board.woods.each do |wood| %>
    <li>
      <div class="ProductList-item">
        <h4 class="ProductList-itemHeading"><%= wood.capitalize %></h4>
        <img class="ProductList-itemImage" src="/boards/<%= @board.slug %>/<%= wood %>.jpg" alt="<%= @board.name %> in <%= wood %>">
      </div>
    </li>
  <% end %>
</ul>
